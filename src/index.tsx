/* import ReactDOM from 'react-dom'; */
import { createRoot } from 'react-dom/client';

import App from './app';

const t = createRoot(document.getElementById('root'))
t.render(<App />)

/* ReactDOM.render(<App />, document.getElementById('root')); */
